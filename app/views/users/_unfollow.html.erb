<%= form_for(current_user.relationships.find_by_followed_id(@user),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "bar", style: "position:relative;width:170px;height:28px;" %>
<% end %>