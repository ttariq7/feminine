

	
  <div href="<%= following_user_path(current_user) %>" style="text-align:center;position:fixed;margin-left:1px;color:#CC337A;">
    <h1 id="following">
      <%= current_user.followed_users.count %>
    </h1>
    <h5 style="font-size:9px;">following</h5>
  </div>

  <div style="text-align:center;position:fixed;margin-top:3px;margin-left:52px;">
	<%= image_tag current_user.avatar_url(:avatar) if current_user.avatar? %>
  </div>

  <div href="<%= followers_user_path(current_user) %>" style="text-align:center;position:fixed;margin-left:109px;">
    <h1 id="followers">
      <%= current_user.followers.count %>
    </h1>
    <h5 style="font-size:9px;">followers</h5>
  </div>
